# FoA example project

This is a very basic Rust project that uses our open source Wi-Fi driver & stack for the ESP32. It only connects to an access point (specified in the source code), makes an HTTP request, and reboots.